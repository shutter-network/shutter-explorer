import ResponsiveLayout from "../layouts/ResponsiveLayout";
import BasicTable from "../components/BasicTable";

const Slot = () => {
    return (
        <ResponsiveLayout>
            <BasicTable />
        </ResponsiveLayout>
    );
};

export default Slot;
